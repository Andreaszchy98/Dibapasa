# No custom secrets or API keys are required for this applet.
# Firebase configuration is loaded automatically via firebase-applet-config.json.
# Maps and routing use open OpenStreetMap (OSM) and OSRM services.
